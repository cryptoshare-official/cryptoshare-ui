import styled from 'styled-components'
import { AppContainer } from '@/styles/css/components/index'

export const Container = styled.section`
    img {
        width: 100%;
    }
`

export const Content = styled(AppContainer)``
